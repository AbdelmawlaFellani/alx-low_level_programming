#include "main.h"

/**
 * add - adds two integers and returns the result
 *
 * @n: the first parametre that we gonna add to the second.
 *
 * @m: the second parametre that we gonna add to the first.
 *
 * Return: the Value of the Sum of those two params
 */
int add(int n, int m)
{
	return (n + m);
}
